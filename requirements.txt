# Everything a directory-only deployment needs...
-r requirements-deploy.txt

# ...plus the voice pipeline: Celery workers on Redis, the ClamAV client, the
# Anthropic client and faster-whisper speech-to-text...
celery
redis
clamd
anthropic
faster-whisper

# ...and the test tools.
pytest
httpx
