# justforyou

FastAPI backend skeleton (Phase 1.1 — no business logic yet).

## Run

Run `docker-compose up --build` then `curl localhost:8000/health` and `curl localhost:8000/health/db`.

Copy `.env.example` to `.env` first if you do not already have one.
